{
  "resourceTemplates": [
    {
      "name": "table",
      "uriTemplate": "db://{connection}/{table}",
      "description": "Action: Read this after db://{connection} to inspect exact table schema (CREATE TABLE syntax) before writing SQL. Use it to confirm column names, types, and constraints.",
      "mimeType": "text/plain"
    }
  ]
}
