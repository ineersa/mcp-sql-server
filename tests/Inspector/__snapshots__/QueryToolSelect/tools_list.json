{
    "tools": [
        {
            "name": "query",
            "description": "Runs read-only SQL queries against chosen database connection.\nOnly SELECT queries are allowed. INSERT, UPDATE, DELETE, DROP, and other write operations are blocked.\n\nSTART HERE - REQUIRED DISCOVERY FLOW:\n- Before writing SQL, read db://{connection} to list available tables for the target connection.\n- Then read db://{connection}/{table} for the exact schema of tables you plan to query.\n- This avoids \"table not found\" and wrong-column errors.\n\nCRITICAL - ROW LIMIT:\n- ALWAYS use exactly 10 rows by default. Never use 20, 50, or 100.\n- MySQL/PostgreSQL/SQLite: Use LIMIT 10\n- SQL Server: Use TOP 10 (LIMIT does not work in SQL Server!)\n\nRULES:\n1. SELECT without WHERE MUST have LIMIT or TOP - queries will be rejected otherwise.\n2. Check the connection type before writing the query - use correct syntax for that database.\n3. For more rows, use pagination with OFFSET.\n4. Large text columns (>200 chars) are truncated to \"<TEXT>\" in multi-row results. To view full text, Query MUST return exactly 1 row.\n\nExamples:\n  MySQL/PostgreSQL/SQLite: SELECT * FROM users LIMIT 10;\n  SQL Server: SELECT TOP 10 * FROM users;\n  Aggregates (no LIMIT needed): SELECT COUNT(*) FROM users;\n\nAvailable connections:\n - local : SQLite, version 3.40.1\n - products : MySQL, version 8.0 [PII GUARDED - sensitive data will be redacted]\n - users : Postgres, version 16.11 (Debian 16.11-1.pgdg13+1)\n - server : SQL Server, version 2019",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "connection": {
                        "type": "string",
                        "description": "the name of the database connection to use"
                    },
                    "query": {
                        "type": "string",
                        "description": "The SQL query (or queries) to execute. Multiple queries must be separated by semicolons."
                    }
                },
                "required": ["connection", "query"]
            },
            "annotations": {
                "title": "Query database",
                "readOnlyHint": true,
                "destructiveHint": true,
                "idempotentHint": false,
                "openWorldHint": false
            }
        },
        {
            "name": "schema",
            "description": "Inspect schema for a database connection.\n\nUse detail=\"summary\" (default) to list matching object names.\nUse detail=\"columns\" to list matching tables with column types.\nUse detail=\"full\" to return full structures (columns, indexes, foreign keys, triggers, check constraints).\n\nFilter note:\n- filter is required and matches object names (tables, views, procedures, functions, sequences).\n- Use filter=\"\" to include all object names.\n\nRecommended flow:\n1. Call with detail=\"summary\" and empty filter to discover names.\n2. Call with detail=\"columns\" to inspect column names and types.\n3. Call again with a refined filter and detail=\"full\" for exact structure.\n\nRoutine note:\n- includeRoutines=true returns stored_procedures, functions, and sequences.\n- In PostgreSQL, many routines are exposed as functions (not procedures).\n\nMetadata note:\n- For direct information_schema/system catalog queries, use real database/schema names not MCP connection aliases.\n\nIf detail=\"columns\" or detail=\"full\" output is too large, the tool returns ToolUsageError and asks for a narrower filter.",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "connection": {
                        "type": "string",
                        "description": "Connection name"
                    },
                    "filter": {
                        "type": "string",
                        "description": "Required object-name filter; empty string means all"
                    },
                    "detail": {
                        "type": "string",
                        "description": "Schema detail level: summary, columns, or full",
                        "default": "summary"
                    },
                    "matchMode": {
                        "type": "string",
                        "description": "Filter matching mode: contains, prefix, exact, glob",
                        "default": "contains"
                    },
                    "includeViews": {
                        "type": "boolean",
                        "description": "Include views in response",
                        "default": false
                    },
                    "includeRoutines": {
                        "type": "boolean",
                        "description": "Include procedures, functions, and sequences in response",
                        "default": false
                    }
                },
                "required": ["connection", "filter"]
            },
            "annotations": {
                "title": "Database Schema structure",
                "readOnlyHint": true,
                "destructiveHint": false,
                "idempotentHint": false,
                "openWorldHint": false
            }
        }
    ]
}
